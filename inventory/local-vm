#
# Hosts on the local machine.
# All currently are VirtualBox VMs.
#
[localhost]
127.0.0.1                  ansible_connection=local
#::1                         ansible_connection=local

# Local hosts (including VMs).
[local:children]
puppet-master
dev-vms
vms

# This is where Ansible lives.
[puppet-master]
debian-sid.vbox.com              ansible_ssh_host=192.168.0.100  ansible_connection=local

[dev-vms]
ubuntu-tahr.vbox.com             ansible_ssh_host=192.168.0.101
ubuntu-vervet.vbox.com           ansible_ssh_host=192.168.0.102

[vms:children]
dev-vms
puppet-master

# Overwrite the ssh user per group:
# [dev-slices:vars]
# ansible_ssh_user=pivanov
# [pivanov.vm3:vars]
# ansible_ssh_user=pivanov